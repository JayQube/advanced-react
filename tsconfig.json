{
  "compilerOptions": {
    // Место куда сохраняется сборка. В данном случае не важна, так как сборка идет через webpack
    "outDir": "./dist/",
    // Подсвечивает все места где не указан тип
    // Однако это не означает, что теперь вовсе нельзя использовать any
    // Это означает лишь, что в подобных ситуациях разработчик должен явно писать any
    "noImplicitAny": true,
    "module": "ESNext",
    // В какую спецификацию компилируем: для поддержки большинства браузеров
    "target": "es5",
    "jsx": "react-jsx",
    // Компилятор будет обрабатывать не только TS файлы, но и JS файлы
    "allowJs": true,
    "strict": true,
    "moduleResolution": "node",
    // Используется для указания базового каталога, относительно которого будут разрешаться модули 
    // при использовании путей без относительных путей (например, import ... from "module" 
    // вместо import ... from "./module").
    // Точка (.) означает текущую директорию (корень проекта, где лежит tsconfig.json).
    "baseUrl": ".",
    // "*" — это шаблон, который означает любой импорт
    // ["./src/*"] — путь, куда TypeScript будет перенаправлять такие импорты
    "paths": {
      "*": ["./src/*"]
    },
    // esModuleInterop позволяет работать с пакетами, которые используют
    // common js как с обычными пакетами с помощью import (require() module.export) = common js
    // import Moment from 'moment';
    // без флага esModuleInterop результат undefined
    // console.log(Moment)
    // с флагом результат [object Object]
    // console.log(Moment)
    "esModuleInterop": true,
    // Если какая-либо библиотека не имеет default exports,
    // лоадеры вроде ts-loader или babel-loader автоматически создают их
    // вместо такого импорта
    // import * as React from 'react';
    // можно писать такой
    // import React from 'react';
    // "plugins": [
    //   {
    //     "name": "typescript-plugin-css-modules"
    //   }
    // ]
    "allowSyntheticDefaultImports": true,
    "plugins": [
      {
        "name": "typescript-plugin-css-modules"
      }
    ]
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  },
  "include": [
    "config/jest/setupTests.tsx",
     "./src/**/*.ts",
     "./src/**/*.tsx"
  ]
}
